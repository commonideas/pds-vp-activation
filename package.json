{
  "name": "pds-vp-activation",
  "version": "2.1.0",
  "private": true,
  "description": "VP magic-link activation — Vercel + Supabase (Panier des Sens)",
  "type": "module",
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.47.0"
  },
  "devDependencies": {
    "vercel": "^39.0.0"
  }
}
